Aim :

To implement a Bluetooth network application that enables file transfer between two Android devices.

This Android application establishes a Bluetooth connection between two devices using Bluetooth sockets. One device acts as a server and the other as a client, allowing file transfer through input and output streams.

Tools Used :

Android Studio

Java

Android Bluetooth API

Result :

The application successfully connects two devices via Bluetooth and transfers files securely between them.

